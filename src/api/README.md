# API

Client-side API adapters live in `src/services`. This folder is reserved for future server/edge route contracts and request schemas so the frontend and Supabase Edge Functions can evolve without coupling UI components to transport details.
