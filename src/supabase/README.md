# Supabase frontend helpers

Keep browser-safe Supabase helpers here when they are specific to the frontend runtime. SQL migrations and database documentation remain in the repository-level `supabase/` directory.
